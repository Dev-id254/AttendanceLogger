/*
  NOTE (repo housekeeping)
  ------------------------
  The active ESP32 firmware sketch lives at:

    Firmware/attendance/attendance.ino

  That location matches the common Arduino sketch folder convention
  (sketch name == folder name). This file remains as a stable pointer
  for older links and documentation.
*/

